# Simulation data
This directory contains the itp-file and an example mdp file used for the production runs. In each subdirectory, a topology file, the input structure file for the production run (`eq_{temperature}_05.gro`) and the final strucutre (`cg_pro_{temperatrue}.gro`) for every simulation can be found.
